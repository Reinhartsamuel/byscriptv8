this is a autotrading saas platform. it is made out of HTML CSS JS and mock data.

create a new branch called "REACT" and pull from main.
create a /frontend folder which is a Vite React js project, Tailwind, ShadcnUI, React query

your task is only one, turn this HTML CSS JS project into React without changing any code on the existing.
only work on the /frontend folder.
everytime you make something, I want you to write log files what you create, what changed. 
so that we have a clear understanding of what has been done, and what has not. Also when we run out of token context, we can continue from the log files.
The log files naming follow this format: `step_X_description.md`. For example, `step_1_create_vite_project.md`.
inside, write a summary of what changed, which file, and progress.

